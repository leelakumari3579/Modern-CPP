#ifndef CARTYPE_H
#define CARTYPE_H

const enum class CarType
{
    SEDAN,
    SUV,
    HATCHBACK,
    OFF_ROADER
};

// enum class MoreVehicleType : public CarType{};  --> inheritance will not work

#endif // CARTYPE_H